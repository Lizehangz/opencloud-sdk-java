# Example

This example shows how a consumer project can use the OpenCloud SDK after the SDK has been installed or published.

## Run Flow

1. Build and install the SDK:

```bash
mvn clean install
```

2. Move into the example project and run your own application code against the installed SDK dependency.

## Example Code

See `src/main/java/com/opencloud/example/ExampleApp.java`.
